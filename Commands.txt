For backend :

PYTHONIOENCODING=utf-8 python3 -m uvicorn backend.main:app --host 0.0.0.0 --port 8000

If port 8000 is already in use, stop the existing backend first or use a different port.

For frontend :

npm run dev
